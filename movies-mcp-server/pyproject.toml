[project]
name = "movies-mcp-server"
version = "0.1.0"
description = "MCP server for fetching movies by genre from TMDB"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

